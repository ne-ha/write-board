<div class="btn btn-default">
    <%= link_to 'Add Notes', new_note_path %>
</div>

<ul>
  <% @notes.each do |note| %>
    <li>
      <%= note.title %>
      <%= link_to 'Show', note_path(note.id) %>
      <%= link_to 'Delete', note_path(note), :method => 'delete', data: {confirm: "Are you sure?"} %>
      <%= link_to 'Share',note_share_note_path(note.id), :remote=>true %>
     </li>
  <% end %>
</ul>


<div id = "modal">

</div>

